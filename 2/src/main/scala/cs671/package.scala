/**
  * CS-671 package.
  * This package contains code that use the assignment-specific packages
  * (applications, performance evaluation. etc.).
  *
  * Unless specified otherwise, code in this package and all sub-packages rejects all invalid
  * arguments, including `null`, with `IllegalArgumentException`.
  */
package object cs671
